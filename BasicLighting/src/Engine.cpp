//#include "Engine.hpp"
//
//int Engine::initialize() {
	// Initialize GLFW
	

	// Create a windowed mode window and its OpenGL context
	//GLFWwindow* window = glfwCreateWindow(800, 600, "Engine Window", NULL, NULL);
	//if (!window) {
	//	std::cerr << "Failed to create GLFW window" << std::endl;
	//	glfwTerminate();
	//	return -1;
	//}
	// Make the window's context current
	//glfwMakeContextCurrent(window);
	// Load OpenGL function pointers using GLAD
	//if (!gladLoadGLLoader((GLADloadproc)glfwGetProcAddress)) {
	//	std::cerr << "Failed to initialize GLAD" << std::endl;
	//	return -1;
	//}
	// Set viewport size
	//glViewport(0, 0, 800, 600);
	// Set callback functions for input handling (optional)
	//glfwSetFramebufferSizeCallback(window, Engine::EventHandler::framebuffer_size_callback);
	//glfwSetKeyCallback(window, Engine::EventHandler::keyboard_callback);
	//glfwSetCursorPosCallback(window, Engine::EventHandler::mouse_callback);
//	return 0; // Initialization successful
//}
